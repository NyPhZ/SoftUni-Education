function greetings(input) {
    let name = (input[0])
    let hours = (input[1]) * 3
    let projects = (input[1])
    let sum =	`The architect ${name} will need ${hours} hours to complete ${projects} project/s.`


    console.log(sum)
}