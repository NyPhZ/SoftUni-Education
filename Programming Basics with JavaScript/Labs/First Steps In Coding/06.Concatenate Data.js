function greetings(input) {
    let a = (input[0])
    let b = (input[1])
    let c = (input[2])
    let d = (input[3])
    let sum = `You are ${a} ${b}, a ${c}-years old person from ${d}.`

    console.log(sum)
}